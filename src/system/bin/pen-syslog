#!/bin/bash
# PENOS Styled System Logs
echo -e "\e[1;33mStreaming System Logs (Press Ctrl+C to stop)...\e[0m"
tail -f /var/log/messages | awk '{
    if ($0 ~ /error/ || $0 ~ /fail/) print "\e[31m" $0 "\e[0m";
    else if ($0 ~ /warn/) print "\e[33m" $0 "\e[0m";
    else print $0;
}'
